#include <stdio.h>
#include <ctype.h>
int void() {
        char a;
        scanf("%c",&a);
        
        if (isupper(a)){
            Printf("Uppercase");
        }
        else{
            Printf("Lowercase");
        }
   
}