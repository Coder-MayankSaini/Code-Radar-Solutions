#include <stdio.h>
#include <ctype.h>
int void() {
        char a;
        scanf("%c",&a,);
        
        if (isupper(a)){
            Print("Uppercase");
        }
        else{
            Print("Lowercase");
        
    }
   
}